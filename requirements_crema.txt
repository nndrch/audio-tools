# Chord detection dependencies — installed into venv_crema by setup.sh
# Do NOT install these into the system Python; they need an isolated env.
#
# Note: setuptools<70 must be installed first (handled by setup.sh).

crema==0.2.0
tensorflow>=2.10,<2.16
keras>=2.10,<3.0
scikit-learn>=0.24,<1.6
pydub>=0.25.1
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24,<2.0

# Symbolic music — MusicXML export (Phase 1) + lead sheet roadmap.
# Pure-Python, no native deps.
music21>=9.1.0

# Render PDF page 1 to PNG for inline preview on the done page (embedded
# PDF viewers are flaky across browsers/sandboxes).
pymupdf>=1.24.0

# Chord-recognition evaluation harness (eval/): MIREX-style weighted chord
# recall. Pure-Python, no native deps.
mir_eval>=0.7
